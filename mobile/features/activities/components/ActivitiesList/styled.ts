import { styled, View } from "tamagui";

export const StyledActivitiesList = {
    Container: styled(View, {
        flex: 1,
        justifyContent: "center",
        gap: "$2",
        width: "100%",
        padding: "$2",
    }),
};
